name: Caller workflow

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Person to greet"
        default: "World"
        required: true
        type: string

jobs:
  greet:
    uses: ./.github/workflows/test_workflow.yaml
    with:
      name: ${{ inputs.name }}
